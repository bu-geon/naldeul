---
deployment:
      tasks:
        - export DEPLOYPATH=/home/naldeulc/public_html
        - /bin/cp /public/index.html $DEPLOYPATH                 #Copy specific file to destination from root
        - /bin/cp /src/index.tsx $DEPLOYPATH   #copy specific file from source sub folder
        - /bin cp * $DEPLOYPATH                           #copy all from root 
        - /bin cp /src/* $DEPLOYPATH             #copy all from sub folder root
        - /bin/cp -r * $DEPLOYPATH                        #copy all recursively to $DEPLOYPATH
        
        